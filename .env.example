# Used only by scripts/export_workflow.py to pull the workflow out of n8n.
# The workflow's own API keys (Apify, Gemini, OpenAI, Google) live in n8n, not here.

N8N_BASE_URL=https://your-instance.app.n8n.cloud
N8N_API_KEY=
